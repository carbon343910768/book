<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.bjtu.xxq.dao.UserDao">

    <resultMap id="BaseResultMap" type="edu.bjtu.xxq.model.User">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="role" jdbcType="VARCHAR" property="role"/>
    </resultMap>

    <select id="findByUsername" resultType="edu.bjtu.xxq.model.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultType="edu.bjtu.xxq.model.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="addUser" resultType="java.lang.Boolean">
        INSERT INTO user (username, password, role) VALUES (#{user.id}, #{user.username}, #{user.password})
    </select>

</mapper>