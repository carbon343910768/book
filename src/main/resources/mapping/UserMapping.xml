<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.bjtu.xxq.dao.UserDao">

    <resultMap id="BaseResultMap" type="edu.bjtu.xxq.model.User">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="role" jdbcType="VARCHAR" property="role"/>
    </resultMap>

    <select id="findAll" resultType="integer">
        SELECT user_id FROM user LIMIT #{start},#{limit};
    </select>

    <select id="findByUsername" resultType="edu.bjtu.xxq.model.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultType="edu.bjtu.xxq.model.User">
        SELECT * FROM user WHERE user_id = #{id}
    </select>

    <select id="findUsersById" resultMap="BaseResultMap" parameterType="java.util.List">
        SELECT.*
        FROM user
        WHERE user_id IN (
        <foreach collection="list" item="id" index="index" separator=",">
            #{id}
        </foreach>
        )
    </select>

    <insert id="addUser">
        INSERT INTO user (username, password, role) VALUES (#{username}, #{password}, #{role})
    </insert>

</mapper>